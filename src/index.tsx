import InstantpayCodePush from './NativeInstantpayCodePush';

export function multiply(a: number, b: number): number {
  return InstantpayCodePush.multiply(a, b);
}

export function getBaseURL(): string {
  return InstantpayCodePush.getBaseURL();
}
